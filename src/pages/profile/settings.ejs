<%
  const meta = {
    title: "Настройки профиля",
    basePath: "../../"
  }

  const layout = "../../layouts/default.ejs"
  const page = "../../components/pages/profile-settings/index.ejs"
%>

<%- include(layout, { body: include(page, { basePath: meta.basePath }), ...meta }) %>
